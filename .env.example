# Flask Configuration
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=production

# Admin Credentials (Multiple accounts supported)
# Account 1 (You)
ADMIN1_USERNAME=username_here
ADMIN1_PASSWORD=your-secure-password-here

# Account 2 (Professor)
ADMIN2_USERNAME=username_here
ADMIN2_PASSWORD=professor-secure-password-here

# Email Configuration (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
EMAIL_FROM=AI Mentor Hub <your-email@gmail.com>
EMAIL_RECIPIENT=your-personal-email@gmail.com

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Firestore Configuration (Recommended - Best for Vercel)
# Method 1: Path to your Firebase credentials JSON file (Local Development)
# Download from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# Method 2: Base64-encoded Firebase credentials (Vercel/Production Deployment)
# To generate: base64 -w 0 firebase-credentials.json (Linux/Mac) or certutil -encode -f firebase-credentials.json temp.b64 && findstr /v /c:- temp.b64 > creds.txt (Windows)
# Then copy the output and paste below (remove line breaks)
FIREBASE_CREDENTIALS_BASE64=your-base64-encoded-credentials-here
